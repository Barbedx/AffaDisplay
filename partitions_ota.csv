# 4MB flash. The default table gives 1.25MB app slots, which the display stack
# plus WiFi plus the web UI already crowd — and BLE would not fit at all. 1.4MB
# each keeps two OTA slots (so a bad flash is always recoverable over the air)
# and still leaves a filesystem for CAN captures.
# Name      Type  SubType  Offset    Size      Flags
nvs,        data, nvs,     0x9000,   0x5000,
otadata,    data, ota,     0xe000,   0x2000,
app0,       app,  ota_0,   0x10000,  0x160000,
app1,       app,  ota_1,   0x170000, 0x160000,
littlefs,   data, spiffs,  0x2D0000, 0x120000,
coredump,   data, coredump,0x3F0000, 0x10000,
